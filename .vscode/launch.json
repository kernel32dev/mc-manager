{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "cargo build debug",
            "name": "(lldb) Debug Client",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/mc-manager.exe",
            "cwd": "${workspaceFolder}",
            "args": [ "run" ],
            "sourceLanguages": [ "rust" ]
        }
    ]
}
